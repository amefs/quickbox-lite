{
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.0",
        "phpstan/phpstan": "^0.12.92"
    },
    "scripts": {
        "lint": ["php-cs-fixer fix -n -vv --dry-run --using-cache no"],
        "lint-fix": ["php-cs-fixer fix -n -vv --using-cache no"],
        "analyse": ["phpstan analyse -c phpstan.neon"]
    },
    "config": {
        "platform": {
            "php": "7.4"
        }
    }
}
